---
description: Stack commands, adapter/runtime constraints, and Svelte MCP tool order for agents
alwaysApply: true
---

# Project stack and Svelte MCP workflow

## Scope (this rule)

**Owns:** **Agent stack facts** (language, package manager, common commands), **SvelteKit deployment/runtime discipline** (adapter, constrained runtimes), **Svelte MCP** tool order, and pointers to ADR entrypoints.

**Does not own:** ESLint/Prettier specifics ([`.cursor/rules/lint-and-code-quality.mdc`](./lint-and-code-quality.mdc)), `.svelte` runes and UI patterns ([`.cursor/rules/svelte-5-ui-conventions.mdc`](./svelte-5-ui-conventions.mdc)), or repo folder map ([`.cursor/rules/project-best-practices.mdc`](./project-best-practices.mdc)).

## Stack (for agents)

- **Language:** TypeScript
- **Package manager:** pnpm
- **Common commands:** `pnpm run dev`, `pnpm run build`, `pnpm run check`, `pnpm run lint`, `pnpm run test`
- **Tooling:** ESLint, Prettier, Vitest, `@sveltejs/adapter-netlify` — portability vs configured adapter: [ADR-006](adrs/ADR-006-serverless-and-secret-boundary.md)
- **Runtime discipline:** Keep server-side code compatible with constrained runtimes; avoid Node-only APIs in shared handlers unless isolated per ADR-006. Prefer SvelteKit [**remote functions**](https://svelte.dev/docs/kit/remote-functions/llms.txt) only when they fit those deployment constraints.

Architecture and boundaries: **`adrs/INDEX.md`** (Accepted ADRs) and **`adrs/GOVERNANCE.md`**.

## Svelte MCP server

When the task involves **Svelte or SvelteKit** (components, runes, routes, loads, SSR), use the **Svelte MCP** tools in this order. For WFD-specific runes, UI, accessibility, and component patterns, follow [`.cursor/rules/svelte-5-ui-conventions.mdc`](./svelte-5-ui-conventions.mdc) after fetching upstream Svelte docs.

### 1. `list-sections`

Call **first** when the task touches Svelte or SvelteKit. Returns sections with titles, `use_cases`, and paths — use it to discover what to read before implementing.

### 2. `get-documentation`

After `list-sections`, **analyze** returned sections (especially `use_cases`) and fetch **all** documentation sections relevant to the task. Do not skip sections that apply to the change.

### 3. `svelte-autofixer`

**Must** run on **every** Svelte file you author or edit **before** finishing the task. **Repeat** until it reports no issues or suggestions.

### 4. `playground-link`

Only after the user **confirms** they want a playground link. **Never** call when code was written into this repository’s files (use local files instead).

---

**Related:** [`.cursor/rules/svelte-5-ui-conventions.mdc`](./svelte-5-ui-conventions.mdc) (WFD `.svelte` patterns, testing pointers).

Human contributors: [`README.md`](../README.md), [`adrs/INDEX.md`](../adrs/INDEX.md), [`.cursor/rules/`](../.cursor/rules/) ([`index.md`](./index.md)).
